{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Wait for app to start",
            "type": "shell",
            "command": "while ! curl --silent --fail localhost:3000; do sleep 0.01; done",
            "problemMatcher": []
        }
    ]
}